buildscript {
    ext.kotlin_version = '1.7.10'
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
    project.evaluationDependsOn(':app')
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

// Move the android block inside the app module's build.gradle file
// D:\OneDrive - Higher Education Commission\Desktop\University\MAD\Semester Project\semester_project\android\app\build.gradle

// Remove the android block from this root build.gradle file
// D:\OneDrive - Higher Education Commission\Desktop\University\MAD\Semester Project\semester_project\android\build.gradle
